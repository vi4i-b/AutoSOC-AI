# Runtime dependencies. Platform-specific packages are guarded with markers.
customtkinter==5.2.2
darkdetect==0.8.0
requests==2.32.5
python-nmap==0.7.1
scapy==2.7.0

# Windows-only: Security event log monitoring (Event ID 4625).
pywin32==311; sys_platform == "win32"
